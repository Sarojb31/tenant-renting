# In local dev, leave unset — Vite proxy handles /api/* → localhost:3000/* (strips /api prefix)
# In production, set to the backend base URL without /api suffix
# (NestJS has no global prefix; routes are /auth/login, /listings, etc.)
# VITE_API_URL=https://api.yourdomain.com
