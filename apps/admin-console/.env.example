# Leave unset in local dev — Vite proxy handles /api → http://localhost:3000
# Set in production/staging to the full API base URL (including /api suffix)
# VITE_API_URL=https://api.yourdomain.com/api
