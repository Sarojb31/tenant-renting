export enum SmsTemplateEvent {
  NEW_MATCH = 'new_match',
  BOOKING_CONFIRMED = 'booking_confirmed',
  RENT_REMINDER = 'rent_reminder',
  OWNER_SUBMISSION_RECEIVED = 'owner_submission_received',
  OWNER_SUBMISSION_APPROVED = 'owner_submission_approved',
  CUSTOM = 'custom',
}
