import { Module } from '@nestjs/common';

// Implements: Plan Section 8 (Booking entity), 14 (/bookings endpoints)
// Build order: Phase 1 Step 6 (alongside Payments)
@Module({})
export class BookingsModule {}
